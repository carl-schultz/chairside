import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: "Poppins",
    text: "Poppins",
  },
  breakpoints: {
    mediumsmall: "777px",
  },
});

export default theme;
